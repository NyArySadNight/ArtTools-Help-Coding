The ArtTools (Vers For Vietnamese)
Prerequisites & Dependencies

To ensure stable execution and full functionality of ArtTools, the following Python packages must be installed: 
pip install PyQt6 requests psutil gputil
System Compiler / Interpreter Requirements

Depending on the languages you plan to run or test within the app, ensure these tools are installed in your system PATH:
Stability & Monitoring Requirements

To ensure proper performance tracking and hardware management within the system manager module:
pip install PyQt6 requests psutil gputil
System Compilers & Interpreters:
g++
Hardware Tooling
nvidia-smi
